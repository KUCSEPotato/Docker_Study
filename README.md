# Docker Study

Docker 학습을 위한 프로젝트입니다.

## 📚 학습 자료
[Notion 학습 노트](https://www.notion.so/Docker-2f81773501178072bdf5d36fcea794de?source=copy_link)

## 📁 프로젝트 구조

```
Docker/
├── README.md
└── Server_Test/           # Node.js Express 서버 Docker 예제
    ├── Dockerfile         # Docker 이미지 빌드 설정
    ├── package.json       # Node.js 의존성 관리
    └── server.js          # Express 서버 코드
```

## 🚀 Server_Test 실행 방법

### 1. Docker 이미지 빌드
```bash
cd Server_Test
docker build -t my-node-server .
```

### 2. Docker 컨테이너 실행
```bash
docker run -p 8080:8080 my-node-server
```

### 3. 서버 접속
브라우저에서 `http://localhost:8080` 접속

## 📝 주요 학습 내용

### Dockerfile 작성 시 주의사항
- **베이스 이미지**: `node:lts-alpine3.23` 사용 (경량화된 Alpine Linux 기반)
- **작업 디렉토리**: `/app`으로 설정
- **의존성 설치 방법**:
  - ❌ `RUN npm install`: OS shell 사용, 보안 취약점 가능
  - ✅ `RUN ["npm", "install"]`: exec form 사용 (권장)
- **포트 노출**: `EXPOSE 8080` (문서화 용도)
- **실행 명령어**: `CMD ["node", "server.js"]`

### Docker 명령어

#### 이미지 관련
```bash
# 이미지 빌드
docker build -t <이미지명> .

# 이미지 목록 조회
docker images

# 이미지 삭제
docker rmi <이미지ID>
```

#### 컨테이너 관련
```bash
# 컨테이너 실행
docker run -p <호스트포트>:<컨테이너포트> <이미지명>

# 백그라운드 실행
docker run -d -p 8080:8080 <이미지명>

# 실행 중인 컨테이너 조회
docker ps

# 모든 컨테이너 조회
docker ps -a

# 컨테이너 중지
docker stop <컨테이너ID>

# 컨테이너 삭제
docker rm <컨테이너ID>

# 컨테이너 로그 확인
docker logs <컨테이너ID>
```

## 🛠 기술 스택
- **Docker**: 컨테이너화 플랫폼
- **Node.js**: JavaScript 런타임
- **Express**: 웹 프레임워크

## 📌 참고사항
- Alpine Linux 기반 이미지를 사용하여 이미지 크기 최소화
- 포트 매핑을 통해 호스트와 컨테이너 간 통신 설정
- exec form을 사용하여 보안성 향상